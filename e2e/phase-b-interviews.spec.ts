import{expect,test}from"@playwright/test";import{createClient}from"@supabase/supabase-js";
const service=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.SUPABASE_SERVICE_ROLE_KEY!,{auth:{persistSession:false}});let vacancyId="",candidateId="",interviewId="";
async function signIn(page:import("@playwright/test").Page,email:string,password:string){await page.goto("/login");await page.getByLabel("Work email").fill(email);await page.getByLabel("Password").fill(password);await page.getByRole("button",{name:"Sign in"}).click();await expect(page).toHaveURL(/\/dashboard$/)}
test.describe("Phase B interview management journey",()=>{
  test.setTimeout(90_000);
  test.afterAll(async()=>{if(candidateId){for(const table of ["notifications","audit_events","activity_logs","candidate_stage_history"])await service.from(table).delete().eq("candidate_id",candidateId)}if(interviewId){await service.from("interview_assignments").delete().eq("interview_id",interviewId);await service.from("interviews").delete().eq("id",interviewId)}if(candidateId)await service.from("candidates").delete().eq("id",candidateId);if(vacancyId){await service.from("vacancy_stages").delete().eq("vacancy_id",vacancyId);await service.from("vacancies").delete().eq("id",vacancyId)}});
  test("HR schedules and assigns; Interviewer receives secure read-only access; Management remains read-only",async({page})=>{const suffix=Date.now();await signIn(page,process.env.E2E_HR_EMAIL!,process.env.E2E_HR_PASSWORD!);await page.goto("/vacancies/new");await page.getByLabel("Title").fill(`Phase B QA ${suffix}`);await page.getByLabel("Department").fill("Quality Assurance");await page.getByLabel("Location").fill("Colombo");await page.getByLabel("Description").fill("Disposable Phase B browser interview management fixture.");await page.getByRole("button",{name:"Create vacancy"}).click();await expect(page).toHaveURL(/\/vacancies\/[a-f0-9-]+$/);vacancyId=new URL(page.url()).pathname.split("/").pop()!;await page.goto("/candidates/new");await page.getByLabel("Vacancy").selectOption(vacancyId);await page.getByLabel("Full name").fill("Phase B Browser Candidate");await page.getByLabel("Email").fill(`phase-b-${suffix}@example.com`);await page.getByLabel("Phone").fill("+94 77 123 9876");await page.getByLabel("Source").fill("Automated QA");await page.getByRole("button",{name:"Create candidate"}).click();await expect(page).toHaveURL(/\/candidates\/[a-f0-9-]+$/);candidateId=new URL(page.url()).pathname.split("/").pop()!;await page.getByRole("link",{name:"Schedule interview"}).click();await page.getByLabel("Recruitment stage").selectOption({label:"Screening"});const future=new Date(Date.now()+172_800_000);const date=`${future.getFullYear()}-${String(future.getMonth()+1).padStart(2,"0")}-${String(future.getDate()).padStart(2,"0")}`;await page.getByLabel("Date").fill(date);await page.getByLabel("Time").fill("10:30");await page.getByLabel("Interview method").selectOption("VIDEO");await page.getByLabel("Meeting link").fill("https://meet.example.com/phase-b-demo");await page.getByLabel("Optional notes").fill("Discuss the candidate’s relevant experience.");await page.getByRole("button",{name:"Schedule interview"}).click();await expect(page).toHaveURL(/\/interviews\/[a-f0-9-]+$/);interviewId=new URL(page.url()).pathname.split("/").pop()!;await expect(page.getByRole("heading",{name:"Screening"})).toBeVisible();await page.getByRole("link",{name:"Assign interviewer"}).click();await page.getByLabel("Ira Patel").check();await page.getByRole("button",{name:"Save assignments"}).click();await expect(page.getByText("Interviewers assigned and notified.")).toBeVisible();await page.getByText("← Back to interview").click();await expect(page.getByText("Ira Patel",{exact:true})).toBeVisible();await page.getByRole("button",{name:"Sign out"}).click();await signIn(page,"interviewer@hirelens.demo",process.env.SEED_INTERVIEWER_PASSWORD!);await expect(page.getByRole("heading",{name:"My assigned interviews"})).toBeVisible();const card=page.getByRole("article").filter({hasText:"Phase B Browser Candidate"});await expect(card).toContainText("Feedback: Pending");await card.getByRole("link",{name:"View interview"}).click();await expect(page.getByRole("heading",{name:"Screening"})).toBeVisible();await expect(page.getByText("Phase B Browser Candidate")).toBeVisible();await expect(page.getByRole("link",{name:"Edit"})).toHaveCount(0);await expect(page.getByRole("link",{name:"Assign interviewer"})).toHaveCount(0);await expect(page.getByRole("button",{name:"Cancel interview"})).toHaveCount(0);await page.getByRole("link",{name:/unread notifications/}).click();await expect(page.getByText("Interview Assigned",{exact:true})).toBeVisible();await expect(page.getByText("Feedback Required",{exact:true})).toBeVisible();await page.getByRole("button",{name:"Sign out"}).click();await signIn(page,"management@hirelens.demo",process.env.SEED_MANAGEMENT_PASSWORD!);await page.goto(`/interviews/${interviewId}`);await expect(page.getByRole("heading",{name:"Screening"})).toBeVisible();await expect(page.getByText("Read-only interview")).toBeVisible();await expect(page.getByRole("link",{name:"Edit"})).toHaveCount(0);await expect(page.getByRole("button",{name:"Cancel interview"})).toHaveCount(0)})
});
