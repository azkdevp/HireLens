import Link from "next/link"; import { requireProfile } from "@/lib/auth"; import { createClient } from "@/lib/supabase/server";
export default async function Dashboard() { const profile=await requireProfile(); const supabase=await createClient(); const [{count:vacancies},{count:candidates}]=await Promise.all([supabase.from("vacancies").select("*",{count:"exact",head:true}).eq("status","ACTIVE"),supabase.from("candidates").select("*",{count:"exact",head:true})]); return <div className="stack"><div><p className="badge">{profile.role.replaceAll("_"," ")}</p><h1>Welcome, {profile.full_name}</h1><p className="muted">Your role-aware Sprint 1 recruitment workspace.</p></div><div className="grid2"><section className="card"><h2>Active vacancies</h2><p style={{fontSize:38,fontWeight:900}}>{vacancies??0}</p>{profile.role==="HR_RECRUITER"&&<Link className="btn" href="/vacancies">Manage vacancies</Link>}</section><section className="card"><h2>Candidates</h2><p style={{fontSize:38,fontWeight:900}}>{candidates??0}</p>{profile.role==="HR_RECRUITER"&&<Link className="btn" href="/candidates">View candidates</Link>}</section></div>{profile.role!=="HR_RECRUITER"&&<section className="card"><h2>Read-only access</h2><p className="muted">Sprint 1 gives your role permission to view recruitment information. Recruitment changes are restricted to HR recruiters.</p><Link className="btn secondary" href="/vacancies">View recruitment information</Link></section>}</div>; }
