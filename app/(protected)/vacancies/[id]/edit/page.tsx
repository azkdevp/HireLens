import {notFound} from "next/navigation";import {requireProfile} from "@/lib/auth";import {createClient} from "@/lib/supabase/server";import {VacancyForm} from "@/components/forms";import type{Vacancy}from"@/lib/types";export default async function Edit({params}:{params:Promise<{id:string}>}){await requireProfile(["HR_RECRUITER"]);const{id}=await params;const s=await createClient();const{data}=await s.from("vacancies").select("*").eq("id",id).eq("status","ACTIVE").single();if(!data)notFound();return <section className="card"><h1>Edit vacancy</h1><VacancyForm vacancy={data as Vacancy}/></section>}
