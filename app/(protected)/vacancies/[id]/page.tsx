import Link from "next/link";import{notFound}from"next/navigation";import{requireProfile}from"@/lib/auth";import{createClient}from"@/lib/supabase/server";import{ArchiveVacancyForm,StagesForm}from"@/components/forms";import type{Stage,Vacancy}from"@/lib/types";
export default async function Detail({params}:{params:Promise<{id:string}>}){const profile=await requireProfile();const{id}=await params;const s=await createClient();const{data}=await s.from("vacancies").select("*,vacancy_stages(*)").eq("id",id).single();if(!data)notFound();const v=data as Vacancy;const stages=[...(v.vacancy_stages??[])].sort((a,b)=>a.stage_order-b.stage_order);return <div className="stack"><section className="card"><div style={{display:"flex",gap:10,alignItems:"start"}}><div style={{marginRight:"auto"}}><span className={`badge ${v.status==="ARCHIVED"?"closed":""}`}>{v.status}</span><h1>{v.title}</h1><p className="muted">{v.department} · {v.location} · {v.employment_type.replaceAll("_"," ")}</p></div>{profile.role==="HR_RECRUITER"&&v.status==="ACTIVE"&&<><Link className="btn secondary" href={`/vacancies/${v.id}/edit`}>Edit</Link><ArchiveVacancyForm vacancyId={v.id}/></>}</div><h2>Description</h2><p style={{whiteSpace:"pre-wrap"}}>{v.description}</p><p className="muted">Created {new Date(v.created_at).toLocaleString()} · Updated {new Date(v.updated_at).toLocaleString()}</p></section><section className="card"><div style={{display:"flex",alignItems:"center"}}><div style={{marginRight:"auto"}}><h2>Recruitment stages</h2><p className="muted">Stages belong only to this vacancy.</p></div><Link className="btn secondary" href={`/pipeline/${v.id}`}>Open pipeline</Link></div>{profile.role==="HR_RECRUITER"&&v.status==="ACTIVE"?<StagesForm vacancyId={v.id} initialNames={stages.map(s=>s.name)}/>:<ol>{stages.map((st:Stage)=><li key={st.id}>{st.name}</li>)}</ol>}</section></div>}
