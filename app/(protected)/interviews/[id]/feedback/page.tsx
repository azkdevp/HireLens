import Link from"next/link";import{notFound}from"next/navigation";import{FeedbackForm}from"@/components/evaluation-forms";import{requireProfile}from"@/lib/auth";import{formatInterviewDate}from"@/lib/interviews";import{createClient}from"@/lib/supabase/server";import type{EvaluationCriterion,FeedbackSubmission,Interview}from"@/lib/types";
export default async function FeedbackPage({params}:{params:Promise<{id:string}>}){const profile=await requireProfile(["INTERVIEWER"]);const{id}=await params;const s=await createClient();const{data}=await s.from("interviews").select("*,candidate:candidates(id,full_name,vacancy:vacancies(id,title)),stage:vacancy_stages(id,name)").eq("id",id).single();if(!data)notFound();const interview=data as unknown as Interview;const[{data:criteria},{data:feedback}]=await Promise.all([s.rpc("effective_evaluation_criteria",{p_vacancy_id:interview.vacancy_id,p_stage_id:interview.stage_id}),s.from("feedback_submissions").select("id,interview_id,stage_id,remarks,strengths,concerns,status,submitted_at,feedback_ratings(criterion_id,criterion_name,criterion_weight,rating)").eq("interview_id",id).eq("author_profile_id",profile.id).maybeSingle()]);const existing=feedback as unknown as FeedbackSubmission|undefined;return <div className="stack"><div><Link className="muted" href={`/interviews/${id}`}>← Back to interview</Link><p><span className={`badge ${existing?.status==="SUBMITTED"?"":"draft"}`}>{existing?.status??"NEW"}</span></p><h1>{existing?.status==="SUBMITTED"?"Submitted feedback":"Complete feedback"}</h1><p className="muted">{interview.candidate?.full_name} · {interview.candidate?.vacancy?.title} · {interview.stage?.name} · {formatInterviewDate(interview.scheduled_at)}</p></div>{existing?.status==="SUBMITTED"?<section className="card"><h2>Evaluation submitted</h2><p className="muted">Submitted evidence is locked and cannot be edited.</p><div className="stack">{existing.feedback_ratings?.map(rating=><div className="score-row" key={rating.criterion_id}><strong>{rating.criterion_name}</strong><span>{rating.rating} / 5{rating.criterion_weight!==null?` · ${rating.criterion_weight}%`:""}</span></div>)}</div><h3>Remarks</h3><p>{existing.remarks}</p><div className="grid2"><div><h3>Strengths</h3><p>{existing.strengths||"None recorded."}</p></div><div><h3>Concerns</h3><p>{existing.concerns||"None recorded."}</p></div></div><p className="muted">Submitted {existing.submitted_at?formatInterviewDate(existing.submitted_at):""}</p></section>:<section className="card">{criteria?.length?<FeedbackForm interviewId={id} criteria={criteria as EvaluationCriterion[]} feedback={existing}/>:<div className="empty-state"><strong>No evaluation criteria configured</strong><p className="muted">HR must configure effective criteria before structured feedback can be completed.</p></div>}</section>}</div>}
