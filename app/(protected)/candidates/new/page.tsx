import{requireProfile}from"@/lib/auth";import{createClient}from"@/lib/supabase/server";import{CandidateForm}from"@/components/forms";export default async function NewCandidate(){await requireProfile(["HR_RECRUITER"]);const s=await createClient();const{data}=await s.from("vacancies").select("id,title").eq("status","ACTIVE").order("title");return <section className="card"><h1>Create candidate</h1>{data?.length?<CandidateForm vacancies={data}/>:<p className="error">Create an active vacancy before adding a candidate.</p>}</section>}
