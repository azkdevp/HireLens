import Link from"next/link";import{notFound}from"next/navigation";import{requireProfile}from"@/lib/auth";import{createClient}from"@/lib/supabase/server";import{moveCandidate}from"@/app/(protected)/actions";import{CandidateOutcomeForm,CvForm}from"@/components/forms";import{StageRemarkForm}from"@/components/evaluation-forms";import{formatInterviewDate}from"@/lib/interviews";import type{Candidate,Interview,Stage}from"@/lib/types";
type Log={id:string;description:string;created_at:string;actor:{full_name:string}|null};type Status={assignment_id:string;full_name:string;feedback_required:boolean;feedback_status:string};type Evidence={id:string;stage_id:string;stage_name:string;interview_id:string|null;author_name:string;remarks:string;strengths:string;concerns:string;submitted_at:string;overall_score:number|null;ratings:{criterion_name:string;criterion_weight:number|null;rating:number}[]};
export default async function CandidateDetail({params}:{params:Promise<{id:string}>}){const profile=await requireProfile();const{id}=await params;const s=await createClient();const{data}=await s.from("candidates").select("*,vacancy:vacancies(id,title),current_stage:vacancy_stages(*)").eq("id",id).single();if(!data)notFound();const c=data as Candidate;const[{data:stages},{data:logs},{data:doc},{data:interviews},{data:evidence}]=await Promise.all([s.from("vacancy_stages").select("*").eq("vacancy_id",c.vacancy_id).order("stage_order"),s.from("activity_logs").select("id,description,created_at,actor:user_profiles!activity_logs_actor_id_fkey(full_name)").eq("candidate_id",id).order("created_at",{ascending:false}),s.from("candidate_documents").select("id,file_name").eq("candidate_id",id).maybeSingle(),s.from("interviews").select("*,stage:vacancy_stages(id,name)").eq("candidate_id",id).order("scheduled_at",{ascending:false}),profile.role!=="INTERVIEWER"?s.rpc("candidate_feedback_evidence",{p_candidate_id:id}):Promise.resolve({data:null})]);const interviewRows=(interviews??[])as Interview[];const statusPairs=await Promise.all(interviewRows.map(async interview=>{const[{data:statuses},{data:completion}]=await Promise.all([s.rpc("interview_feedback_status",{p_interview_id:interview.id}),s.rpc("feedback_completion",{p_candidate_id:id,p_stage_id:interview.stage_id})]);return[interview.id,{statuses:(statuses??[])as Status[],completion:completion?.[0]}]as const}));const feedback=new Map(statusPairs);const ordered=(stages??[])as Stage[];const index=ordered.findIndex(x=>x.id===c.current_stage_id);const next=ordered[index+1];const hr=profile.role==="HR_RECRUITER";const evidenceRows=(evidence??[])as Evidence[];return <div className="stack"><section className="card"><div style={{display:"flex",alignItems:"start",gap:10}}><div style={{marginRight:"auto"}}><span className={`badge ${c.status==="CLOSED"?"closed":""}`}>{c.outcome??c.status}</span><h1>{c.full_name}</h1><p className="muted">{c.email} · {c.phone}</p></div>{hr&&<Link className="btn secondary" href={`/candidates/${id}/edit`}>Edit</Link>}</div><div className="grid2"><div><h3>Vacancy</h3><p>{c.vacancy?.title}</p></div><div><h3>Current stage</h3><p>{c.current_stage?.name}</p></div><div><h3>Source</h3><p>{c.source}</p></div><div><h3>Notes</h3><p style={{whiteSpace:"pre-wrap"}}>{c.notes||"No notes"}</p></div></div></section>{hr&&<section className="card grid2"><div><h2>Progress candidate</h2>{next&&c.status==="ACTIVE"?<form action={moveCandidate}><input type="hidden" name="candidate_id" value={id}/><input type="hidden" name="target_stage_id" value={next.id}/><button className="btn">Move to {next.name}</button></form>:<p className="muted">No next-stage transition is available.</p>}</div><div><h2>Record outcome</h2><CandidateOutcomeForm candidateId={id} outcome={c.outcome}/></div></section>}<section className="card"><div className="section-heading"><div><h2>Interviews</h2><p className="muted">Scheduled interviews and feedback completion.</p></div>{hr&&c.status==="ACTIVE"&&<Link className="btn" href={`/candidates/${id}/interviews/new`}>Schedule interview</Link>}</div>{interviewRows.length?<div className="stack">{interviewRows.map(interview=>{const state=feedback.get(interview.id);return <article className="interview-card" key={interview.id}><div><span className={`badge ${interview.status==="CANCELLED"?"closed":""}`}>{interview.status}</span><h3>{interview.stage?.name}</h3><p>{formatInterviewDate(interview.scheduled_at)} · {interview.method.replaceAll("_"," ")}</p><p className="muted">Assigned: {state?.statuses.map(item=>`${item.full_name} — ${item.feedback_status}`).join(", ")||"None"}</p><strong>{state?.completion?.required_count?`${state.completion.submitted_count} / ${state.completion.required_count} feedback complete`:"Feedback not required"}</strong></div><Link className="btn secondary" href={`/interviews/${interview.id}`}>View interview</Link></article>})}</div>:<div className="empty-state"><strong>No interviews scheduled</strong><p className="muted">Interview history will appear here.</p></div>}</section>{profile.role!=="INTERVIEWER"&&<section className="card"><div className="section-heading"><div><h2>Stage evidence</h2><p className="muted">Submitted remarks and human evaluation evidence remain historically visible.</p></div></div>{evidenceRows.length?<div className="stack">{evidenceRows.map(item=><article className="evidence-card" key={item.id}><div className="section-heading"><div><span className="badge">{item.interview_id?"Interview feedback":"Stage remark"}</span><h3>{item.stage_name}</h3></div>{item.overall_score!==null&&<div className="overall-score"><strong>{item.overall_score}</strong><small>/ 5 overall evaluation</small></div>}</div>{item.ratings.length>0&&<div className="stack">{item.ratings.map(rating=><div className="score-row" key={rating.criterion_name}><strong>{rating.criterion_name}</strong><span>{rating.rating} / 5{rating.criterion_weight!==null?` · ${rating.criterion_weight}%`:""}</span></div>)}</div>}<h4>Remarks</h4><p style={{whiteSpace:"pre-wrap"}}>{item.remarks}</p>{item.interview_id&&<div className="grid2"><div><h4>Strengths</h4><p>{item.strengths||"None recorded."}</p></div><div><h4>Concerns</h4><p>{item.concerns||"None recorded."}</p></div></div>}<p className="muted">— {item.author_name} · {formatInterviewDate(item.submitted_at)}</p></article>)}</div>:<div className="empty-state"><strong>No stage evidence recorded</strong><p className="muted">Submitted remarks and interview feedback will appear here.</p></div>}{hr&&<details className="remark-panel"><summary>Add stage remark</summary><StageRemarkForm candidateId={id} stages={ordered}/></details>}</section>}<section className="card"><h2>Candidate CV</h2>{doc?<p><Link className="btn secondary" href={`/api/candidates/${id}/cv`}>Download {doc.file_name}</Link></p>:<p className="muted">No CV is attached.</p>}{hr&&<CvForm candidateId={id}/>}</section><section className="card"><h2>Activity timeline</h2>{logs?.length?<ol className="stack" style={{listStyle:"none",padding:0}}>{(logs as unknown as Log[]).map(log=><li key={log.id} style={{borderLeft:"3px solid #64a18d",paddingLeft:14}}><strong>{log.description}</strong><div className="muted">{new Date(log.created_at).toLocaleString()} · {log.actor?.full_name??"System"}</div></li>)}</ol>:<p className="muted">No activity has been recorded.</p>}</section></div>}
