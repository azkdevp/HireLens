import{notFound}from"next/navigation";import{requireProfile}from"@/lib/auth";import{createClient}from"@/lib/supabase/server";import{CandidateForm}from"@/components/forms";import type{Candidate}from"@/lib/types";export default async function EditCandidate({params}:{params:Promise<{id:string}>}){await requireProfile(["HR_RECRUITER"]);const{id}=await params;const s=await createClient();const[{data:c},{data:v}]=await Promise.all([s.from("candidates").select("*").eq("id",id).single(),s.from("vacancies").select("id,title").order("title")]);if(!c)notFound();return <section className="card"><h1>Edit candidate</h1><CandidateForm vacancies={v??[]} candidate={c as Candidate}/></section>}
